// const dayjs = require("dayjs");

// const calculateDate = (born) =>{
//   const bornDate = dayjs(born)
//   const actYear = dayjs()
//   const actDay = dayjs().format('DD/MM/YYYY')
  
//   const years = actYear.diff(bornDate, 'year')
//   const birthday = bornDate.add(years + 1, 'year')
//   const daysToNext = birthday.diff(actDay, 'day')

//   console.log(`Você tem ${years} anos e sua próxima data de aniversário sera em ${birthday.format("DD/MM/YYYY")} e faltam ${daysToNext} para seu próximo aniversário`);
// };

// calculateDate("2001/06/5");
